# include "main.h"
/**
 * _islower : checks whether a character is small
 *
 * Return : Always 0.
 */
int _islower(int c)
{
	int i;

	for (i = 65 ; i < 123 ; i++)
	{
		if (i == c)
		{
			break;
			return (1);
		}
		else
		{
			return (0);
		}
	return (0);
}
