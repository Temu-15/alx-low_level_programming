# include "main.h"
/**
 * print_sign - prnts the sign of a number
 * @n: takes input as an integer
 *
 * Return: Always 0.
 */
int print_sign(int n)
{
	char ch[] = "+";
	char neg[] = "-"
	if (n > 0)
	{
		_putchar(ch[]);
		return (1);
	}
	if else (n == 0)
	{ 
		_putchar(0);
		return (0);
	}
	else
	{
		_putchar(neg[]);
		return (-1);
	}
	return (0);
}
