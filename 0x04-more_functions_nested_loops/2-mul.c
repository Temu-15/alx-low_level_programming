# include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: takes the two integers asn an input
 * @b: takes the second number to be multiplied
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int d;

	d = a * b;
	_putchar(d);
	return (0);
}
